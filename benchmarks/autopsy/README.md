# Round 10 Mechanism Autopsy

The four directories are evaluator-side autopsy templates and trace schemas.
They are not imported by runtime code and do not contain human reference
reports. The current records are explicitly `BLOCKED_REAL_RUN` because the
Docker Linux engine is unavailable; they must be replaced by captured traces
after a real static-only sample run.
