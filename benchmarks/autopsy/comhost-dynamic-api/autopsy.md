# ComHost Dynamic API Autopsy

Status: `BLOCKED_REAL_RUN`. The evaluator schema is ready, but no ComHost
mechanism result is claimed until a real isolated static run is available.
Root cause: `TOOL_RESULT_FAILURE` due Docker engine unavailability.
