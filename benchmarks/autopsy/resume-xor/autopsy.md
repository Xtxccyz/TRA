# Resume XOR Autopsy

Status: `BLOCKED_REAL_RUN`. The required semantic chain and funnel stages were
not claimed from a fixture. Root cause: `TOOL_RESULT_FAILURE`, because the
isolated product worker could not be started while the Docker Linux engine was
unavailable. Re-run this autopsy after a real static-only Resume submission.
