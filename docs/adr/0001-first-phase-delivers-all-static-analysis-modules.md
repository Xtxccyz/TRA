# 首期交付完整静态分析模块

需求文档原计划首期只交付静态分诊、解密和加载器三个核心 Agent，但这不足以稳定实现“提交样本包后生成接近人工基准的完整报告”。首期改为交付 Step 0-6 的全部静态分析模块及全部报告模块；Unicorn、沙箱和其他动态执行能力仍留到后续阶段，这一范围优先于需求文档中的原阶段划分。

## Consequences

首期不能再按“三 Agent 原型”验收，排期和任务拆分需要覆盖 C2、反分析、证据聚合与归因等模块。任何模块都不得用推测代替暂未交付的动态证据。
