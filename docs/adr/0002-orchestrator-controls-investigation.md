# 调查控制权归编排器

动态调查图由确定性策略引擎和编排器控制，而不是由任一 LLM Agent 直接控制。Agent只能提交带目标、理由、预期证据、成本与风险的下一步提议；策略引擎负责自动批准、转人工Gate或拒绝，编排器负责更新案件状态、调度任务、去重、预算、重试和终止，人工保留最终覆盖权。

## Consequences

Agent不得自行改变Case范围、完成条件、资源预算或工具权限，所有提议和裁决都必须审计。首期未知代码执行、样本联网和其他动态行为由外部策略直接拒绝，不能通过Prompt或Agent决定绕过。
