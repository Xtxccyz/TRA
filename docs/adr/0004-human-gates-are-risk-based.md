# 人工Gate按风险分级而非全程阻塞

首期分析默认自动推进，人工只在输入越界、核心证据冲突、资源软预算、知识晋升和外部发布等风险边界介入；动态执行请求在首期直接拒绝，不作为可临时批准的入口。Agent自动生成可查看的DRAFT报告，分析状态、人工批准状态和外部发布状态分别管理。

## Consequences

局部争议只阻塞相关Claim及其依赖，资源硬上限不可被Agent或普通审核人员绕过。后续启用动态执行、知识闭环和检测生产时可以增加审批权限与执行节点，但不得削弱既有审计和隔离边界。
