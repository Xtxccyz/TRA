# 输入通道既隔离也对用户可见

每个Case把任务请求、样本包、背景上下文和知识快照作为四类独立输入；前端可以保留对话体验，但一期界面和报告审计信息必须显式展示四通道输入清单、来源与版本。评测基准报告不进入任何分析通道，解压密码等敏感值通过独立秘密字段处理。

## Consequences

四类输入分别执行权限、来源和证据性质校验，用户文本不能直接成为样本事实或任意工具命令。后续新增沙箱策略、网络权限和客户情报时，仍应进入对应的结构化通道，而不是扩充一个无边界聊天上下文。
