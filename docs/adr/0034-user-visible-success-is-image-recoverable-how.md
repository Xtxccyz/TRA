# 用户可见成功是镜像内可恢复 HOW，不是 UNKNOWN 过关

Complete 静态方案把 C10 写成「HOW 或显式 UNKNOWN」，主工程据此标 `complete_c10_passed`，3080 上几乎看不到相对评测基准报告的深度变化。现改为：镜像内可恢复的能力类型槽位停在 UNKNOWN 时不得标用户验收完成；只有运行时事实在真实受控模拟之后可以 UNKNOWN。旧 C10 绿灯只证明诚实门和契约，不证明用户要的分析报告已达到。
